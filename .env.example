# Event Service Agent - Environment Configuration
#
# Copy this file to .env and customize for your environment:
#   cp .env.example .env
#
# Note: .env is gitignored - do not commit credentials

# ============================================================================
# Database Configuration (Platform)
# ============================================================================

# Database file path
# Default: <workspace>/data/db.sqlite
# Production: /var/lib/myapp/db.sqlite
# Testing: :memory: or /tmp/test-db.sqlite
DB_PATH=./data/db.sqlite

# Schema dump directory (SQL schema snapshots after migrations)
# Default: <workspace>/data
DB_SCHEMA_DIR=./data

# ============================================================================
# Migration Scripts
# ============================================================================
# The above DB_* variables are used by:
#   bun --filter @event-service-agent/platform migrate
#   bun --filter @event-service-agent/platform reset

# ============================================================================
# Future Configuration (not yet implemented)
# ============================================================================

# Log level (trace, debug, info, warn, error)
# LOG_LEVEL=info

# Message broker URL (when we implement Kafka/Redis)
# BROKER_URL=kafka://localhost:9092

# HTTP server port
# PORT=3000
