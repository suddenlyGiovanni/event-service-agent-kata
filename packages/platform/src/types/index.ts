/**
 * Shared Types Exports
 *
 * HTTP types for request specifications.
 * For message envelope types, use MessageEnvelopeSchema.Type from @event-service-agent/schemas/envelope
 */

export type { HttpMethod, RequestSpec, RequestSpecWithoutBody } from './http.type.ts'
